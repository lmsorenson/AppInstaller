language: c++
compiler:
  - clang
jobs:
  include:
    - os: osx
      osx_image: xcode12.2
addons:
  homebrew:
    packages:
    - qt
    - qtbase5-dev
    - qtdeclarative5-dev
    update: true
before_script:
  - mkdir build
  - ls -la
  - cd build
script:
  - cmake ..
